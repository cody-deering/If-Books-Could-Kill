<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>If Books Could Kill</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
        crossorigin="anonymous">
    <style>
        body {
            padding-top: 2rem;
            text-align: center;
            font-family: Arial, sans-serif;
            background-color: rgb(0, 0, 0);
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .navbar {
            margin-bottom: 20px;
        }

        .navbar-brand {
            color: red;
        }

        .navbar-search {
            position: absolute;
            top: 0;
            right: 380px;
            margin-top: 10px;
        }

        .genre-nav {
            margin-top: 20px;
        }

        .book {
            margin-bottom: 20px;
        }

        .search-btn {
            color: red;
            border: none;
            background-color: black;
            width: 9rem;
        }
    </style>
</head>

<body>

    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="#">If Books Could Kill</a>
            <div class="mx-auto">
                <form class="form-inline" style="display: flex;">
                    <input class="form-control" type="text" name="query" placeholder="Search Books">
                    <button class="search-btn" type="submit">Search</button>
                </form>
            </div>
            <button class="btn btn-info cart-button">
                Cart <span class="badge">0</span>
            </button>
        </div>
    </nav>

    <div class="container">
        <nav class="genre-nav" style="padding-bottom: 2rem;">
            <ul class="nav justify-content-center">
                <li class="nav-item">
                    <a class="nav-link" href="/books?genre=Mystery+Crime+Drama" style="color: red;">Mystery/Crime Drama</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/books?genre=Horror" style="color: red;">Horror</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/books?genre=Thriller"style="color: red;">Thriller</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/books?genre=True+Crime"style="color: red;">True Crime</a>
                </li>
            </ul>
        </nav>
    </div>

    <div class="container">
        <div class="row">
            {{#each books}}
            <div class="col-md-4 mb-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text">Author: {{author}}</p>
                        <p class="card-text">Description: {{description}}</p>
                        <p class="card-text">Genres: {{genres}}</p>
                        <p class="card-text">Average Rating: {{average_rating}}</p>
                        <p class="card-text">Ratings Count: {{ratings_count}}</p>
                        <button class="btn btn-danger add-to-cart-button" data-book-id="{{id}}">Add to Cart</button>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <script src="../../models/books.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js"
        integrity="sha384-Rks4IEzC6M9zPfSO2w0f8FLZR5s5+Mq8gEBJw7l3SxoJSmpeb+KFiXFnAg5ww5qb" crossorigin="anonymous"></script>
</body>

</html>
