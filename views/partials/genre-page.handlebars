<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>If Books Could Kill</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>
  <body>

    <nav class="navbar">
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand" href="#">If Books Could Kill</a>
        </div>

        <div class="navbar-search">
          <form class="form-inline">
            <input
              class="form-control"
              type="text"
              name="query"
              placeholder="Search Books"
            />
            <button
              class="btn btn-default search-btn"
              type="submit"
            >Search</button>
          </form>
        </div>

        <div class="profile-window">
          <button class="btn btn-info cart-button">
            Cart
            <span class="badge">0</span>
          </button>
        </div>
      </div>
    </nav>

    <style>
      body { display: flex; flex-direction: column; justify-content: center;
      padding-top: 70px; text-align: center; font-family: Arial, sans-serif;
      background-color: rgb(0, 0, 0) } .navbar-brand { color: red; } .search-btn
      { color: red; border: none; } .navbar-search{ position: relative; }
      .cart-button { color: red; border-color: red; background-color: black; }
    </style>

    <!-- The rest of your genre page content goes here -->
    <div class="container">
      <div class="genre-text row justify-content-center">
        <div class="col">
          <a href="/books?genre=Mystery" style="color: red;">Mystery/Crime Drama</a>
        </div>
        <div class="col">
          <a href="/books?genre=Horror" style="color: red;">Horror</a>
        </div>
        <div class="col">
          <a href="/books?genre=Thriller" style="color: red;">Thriller</a>
        </div>
        <div class="col">
          <a href="/books?genre=True+Crime" style="color: red;">True Crime</a>
        </div>
      </div>
    </div>
    {{#each books}}
      <div class="book">
        <h2>{{title}}</h2>
        <p>Author: {{author}}</p>
        <p>Description: {{description}}</p>
        <p>Genres: {{genres}}</p>
        <p>Average Rating: {{average_rating}}</p>
        <p>Ratings Count: {{ratings_count}}</p>

        <button
          class="btn btn-danger add-to-cart-button"
          data-book-id="{{id}}"
        >Add to Cart</button>

      </div>
    {{/each}}
    <script src="../../models/books.js"></script>
  </body>
</html>